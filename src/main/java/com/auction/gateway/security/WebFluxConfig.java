package com.auction.gateway.security;



//@Configuration
public class WebFluxConfig {
/*
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        // Allow CORS for specific endpoints
        registry.addMapping("/api/**")
                .allowedOrigins("http://localhost:4200") // Allow the Angular app to make requests
                .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS") // Allow specific HTTP methods
                .allowedHeaders("Authorization", "Content-Type") // Allow specific headers
                .allowCredentials(true); // Allow credentials (cookies, headers, etc.)
    }
    */
}
